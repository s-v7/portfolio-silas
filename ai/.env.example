# Provider da LLM (inicialmente só OpenAI)
LLM_PROVIDER=openai

# Chave da API (NUNCA versionar a real)
LLM_API_KEY=sk-your-api-key-here

# Modelo padrão
LLM_MODEL=gpt-4o-mini

# Limite de tokens por resposta
LLM_MAX_TOKENS=1200

# Temperatura da geração
LLM_TEMPERATURE=0.4

